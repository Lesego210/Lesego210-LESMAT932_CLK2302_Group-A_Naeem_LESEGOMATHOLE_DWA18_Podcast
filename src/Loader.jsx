import React from 'react';
import './Loader.css'; // Ensure you have this CSS file in the same directory or adjust the path accordingly

const Loader = () => (
  <div className="loader-container">
    <div className="loader"></div>
  </div>
);

export default Loader;
